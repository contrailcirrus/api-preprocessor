# image is the docker image uri to be deployed including the image tag
image:

# sourcePath is the fully-qualified file path for HRES pressure-level zarr stores
sourcePath:
# sinkPath is the fully-qualified file path prefix for writing output data assets
sinkPath:
# apiPreprocessorSubscriptionId is the subscription for api-preprocessor jobs (generated by the hres-etl svc)
apiPreprocessorSubscriptionId:
